{
  "name": "ytdlp-api",
  "version": "1.0.0",
  "description": "yt-dlp API Microservice (Auto-Updating)",
  "main": "index.js",
  "scripts": {
    "prestart": "pip install -U yt-dlp || pip3 install -U yt-dlp || echo 'Update skipped'",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
